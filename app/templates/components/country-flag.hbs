{{#link-to 'country' country.id}}
	{{#if country.flag.url}}
		{{#tool-tipster title=country.name position="bottom"}} 
		<img {{bind-attr src=country.flag.url}} width="50px">
	  {{/tool-tipster}}
	{{/if}}
{{/link-to}}
					
